# Directus backend for Railway.
# Set the Railway service "Root Directory" to: directus
# All real configuration is done via Railway environment variables (see below).
FROM directus/directus:11

# Railway provides $PORT at runtime; Directus reads PORT automatically.
EXPOSE 8055

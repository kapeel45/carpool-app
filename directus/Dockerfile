# Directus 11 — used by DigitalOcean App Platform / Droplet / Railway
# Set source / root directory to: directus
FROM directus/directus:11

# App Platform and most hosts inject PORT at runtime.
# Directus listens on PORT when set; default is 8055.
EXPOSE 8055
